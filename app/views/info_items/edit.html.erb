<!-- this could be partialized too! -->
<h1>Edit Topic</h1>

<div class="form">
  <%= form_for [@info_item.report, @info_item] do |f| %>

  <%= f.label :category %>
  <%= f.text_field :category %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Edit Topic" %>
  <% end %>
</div>
