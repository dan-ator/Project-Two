<h1>New Topic</h1>

<div class="form">
  <%= form_for [@report, @info_item] do |f| %>

  <%= f.label :category %>
  <%= f.text_field :category %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Add Topic" %>
  <% end %>
</div>
